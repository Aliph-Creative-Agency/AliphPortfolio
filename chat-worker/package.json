{
  "name": "aliph-chat-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Aliph Creative chat widget backend — Cloudflare Worker. See ../aliph-chatbot-plan.md",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
